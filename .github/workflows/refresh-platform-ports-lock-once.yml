name: Refresh platform-ports lock once

on:
  push:
    branches:
      - docs/platform-ports-draft

permissions:
  contents: write

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - name: Check out branch
        uses: actions/checkout@v6
        with:
          ref: docs/platform-ports-draft
          fetch-depth: 0
      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24
      - name: Enable pinned npm
        env:
          COREPACK_ENABLE_DOWNLOAD_PROMPT: "0"
        run: |
          corepack enable npm
          test "$(npm --version)" = "10.9.2"
      - name: Refresh workspace lock metadata
        env:
          COREPACK_ENABLE_DOWNLOAD_PROMPT: "0"
        run: npm install --package-lock-only --ignore-scripts --no-audit --no-fund
      - name: Commit lockfile only
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add package-lock.json
          if git diff --cached --quiet; then
            exit 0
          fi
          git commit -m "chore(platform-ports): sync workspace lock metadata"
          git push origin HEAD:docs/platform-ports-draft
