name: Finalize platform-ports docs once

on:
  push:
    branches: [feat/platform-ports-m4]

permissions:
  contents: write

jobs:
  finalize:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: feat/platform-ports-m4
          fetch-depth: 0
      - name: Close platform-ports documentation ownership
        run: |
          python - <<'PY'
          from pathlib import Path

          def replace(path, old, new):
              p = Path(path)
              text = p.read_text()
              if old not in text:
                  raise SystemExit(f"expected text not found in {path}: {old[:120]!r}")
              p.write_text(text.replace(old, new, 1))

          # package-architecture.md: make platform-ports a first-class contract package.
          path = 'doc/30-implementation/package-architecture.md'
          replace(path,
'''## 3. Document / Contract Capability Packages

```text
@loomrealm/game-package
@loomrealm/runtime-control
@loomrealm/renderer-control
@loomrealm/data
@loomrealm/content
```''',
'''## 3. Contract Capability Packages

```text
@loomrealm/game-package
@loomrealm/runtime-control
@loomrealm/renderer-control
@loomrealm/data
@loomrealm/content
@loomrealm/platform-ports
```''')
          replace(path,
'''Profile composition does not merge child identity/lifecycle/authority。

---

## 4. Platform-neutral Role Packages''',
'''Profile composition does not merge child identity/lifecycle/authority。

### `platform-ports`

Position：platform-neutral Core ↔ Platform capability contract boundary；它定义 Core 需要平台提供的窄 capability/fact，不拥有 Core authority、role policy、protocol mechanics 或 concrete Hostra/PWA implementation。

M4 frozen root surface exactly：

```text
DeadlineScheduler
RuntimeControlBinding
```

Runtime dependency exactly：

```text
@loomrealm/foundation
```

`DeadlineScheduler` 与 Runtime Control scheduler structural-compatible，但 `platform-ports` MUST NOT依赖 `@loomrealm/runtime-control`。`RuntimeControlBinding` 是 one-Launch-Attempt / single-use / no-reconnect establishment capability。

M5+ Main/Renderer/Data/Content ports 只在对应 real consumer closure 时增长；不得提前建立万能 `Platform` object、service locator 或 future port inventory。

---

## 4. Platform-neutral Role Packages''')
          replace(path,
'''Trusted integration：

```text
@loomrealm/subsystem/host
    runSubsystem
    RuntimeControlBinding
    SubsystemDataBinding
    SubsystemLaunchContext
```

`@loomrealm/subsystem/host` is the first real Subsystem-side consumer of Runtime Control typed peer；it maps protocol mutation-pending state into ordinary-input gating/business control flow。''',
'''Trusted integration：

```text
@loomrealm/subsystem/host
    runSubsystem
    SubsystemLaunchContext
    SubsystemRuntimeControlPolicy
```

M4 Platform capability contract 由 `@loomrealm/platform-ports` 唯一拥有：

```text
DeadlineScheduler
RuntimeControlBinding
```

`@loomrealm/subsystem/host` consumes these ports and is the first real Subsystem-side consumer of Runtime Control typed peer；it owns role-local deadline policy and maps protocol mutation-pending state into ordinary-input gating/business control flow。`SubsystemDataBinding` exact Platform contract deferred to M8；M4 MUST NOT预定义 fake Data port。''')
          replace(path,
'''| Host Runtime Control role mapping | Runtime Control v1 | M4 |
| DataPlane + `SubsystemDataBinding` application integration | Renderer Data Profile | M8 |''',
'''| Host Runtime Control role mapping | `@loomrealm/platform-ports` M4 slice + Runtime Control v1 | M4 |
| DataPlane + future Subsystem Data binding application integration | Renderer Data Profile + M8 Platform port closure | M8 |''')
          replace(path,
'''Thus：

- Runtime Control scheduler remains local M3 port while only Runtime Control needs it；
- `SubsystemDataBinding` may live in subsystem host surface；
- `RendererDataBinding` may live in renderer integration surface；
- `DataConnectionBroker` stays outside subsystem；''',
'''Thus：

- M4 `DeadlineScheduler` / `RuntimeControlBinding` live in `@loomrealm/platform-ports` because Core consumes them while Hostra/PWA may realize them differently；
- Runtime Control keeps its own structural scheduler type and does not become a dependency of `platform-ports`；
- `SubsystemDataBinding` exact Platform contract is deferred to M8 and is not an M4 host-owned placeholder；
- `RendererDataBinding` exact placement waits for its real consumer closure；
- `DataConnectionBroker` stays outside subsystem；''')
          replace(path,
'''foundation ───────────────┐
                          ↓
wire ─────────────→ runtime-control
 │                        ↓
 │                main / subsystem-host''',
'''foundation ─────→ platform-ports ─────→ main / subsystem-host
 │                        
 ├───────────────────────┐
 │                       ↓
wire ─────────────→ runtime-control
 │                        ↓
 │                main / subsystem-host''')
          replace(path,
'''subsystem
    author root → data/content/foundation as exposed by SDK
    host        → runtime-control + role-local integrations''',
'''subsystem
    author root → data/content/foundation as exposed by SDK
    host        → platform-ports + runtime-control + role-local policy/integrations''')
          replace(path,
'''packages/
├── foundation/
├── wire/''',
'''packages/
├── foundation/
├── platform-ports/
├── wire/''')
          replace(path, '> 最近复核：2026-08-21', '> 最近复核：2026-08-27')

          # repository-layout.md: reflect package and remove false subsystem ownership files.
          path = 'doc/30-implementation/repository-layout.md'
          replace(path,
'''packages/
├── foundation/
├── wire/''',
'''packages/
├── foundation/
├── platform-ports/
├── wire/''')
          replace(path,
'''low-level
    foundation / wire

document/contract capability
    game-package / runtime-control / renderer-control / data / content''',
'''low-level
    foundation / wire

platform capability contract
    platform-ports

document/contract capability
    game-package / runtime-control / renderer-control / data / content''')
          replace(path,
'''---

## 3. `packages/game-package`''',
'''### `packages/platform-ports`

M4 current layout：

```text
packages/platform-ports/
├── DESIGN.md
├── package.json
├── tsconfig.json
└── src/
    └── index.ts
```

M4 root export exactly：

```text
DeadlineScheduler
RuntimeControlBinding
```

Runtime dependency exactly `@loomrealm/foundation`。它是 Platform capability contract owner，不包含 Hostra/PWA implementation、role policy、protocol mechanics 或万能 Platform object。

---

## 3. `packages/game-package`''')
          replace(path,
'''│   ├── host/
│   │   ├── run-subsystem.ts
│   │   ├── runtime-control-binding.ts
│   │   └── platform-ports.ts
│   └── internal/''',
'''│   ├── host/
│   │   ├── run-subsystem.ts
│   │   ├── runtime-control-plane.ts
│   │   └── runtime-policy.ts
│   └── internal/''')
          replace(path,
'''M4 `runtime-control-binding.ts` is the first real Subsystem-side consumer of `SubsystemRuntimeControlPeer`；it maps protocol call/return pending state to ordinary-input gating and maps typed outcomes into Frame business control flow。''',
'''M4 host implementation imports `DeadlineScheduler` / `RuntimeControlBinding` from `@loomrealm/platform-ports` and consumes `SubsystemRuntimeControlPeer` internally；Subsystem owns only host orchestration / role policy / protocol-to-business mapping，不在本地重复定义 Platform port contract。''')
          replace(path, '> 最近复核：2026-08-21', '> 最近复核：2026-08-27')

          # phase-1-delivery-plan.md: make M4 platform-port gate explicit and defer Data port to M8.
          path = 'doc/30-implementation/phase-1-delivery-plan.md'
          replace(path,
'''### Host Runtime slice

Implement：

```text
runSubsystem
RuntimeControlBinding using SubsystemRuntimeControlPeer
SubsystemLaunchContext
Runtime Control role mapping
```

`SubsystemDataBinding` 可作为完整 host boundary 的 port shape 被定义/保留，但 M4 MUST NOT通过 fake DataPlane/Input/Render behavior宣称其 application semantics 已关闭；真正 Data application integration从 M8开始。''',
'''### Host Runtime slice

M4 Platform capability contract baseline（已由 `@loomrealm/platform-ports` package-local implementation 提供，real consumer qualification 在本 milestone 关闭）：

```text
@loomrealm/platform-ports
    DeadlineScheduler
    RuntimeControlBinding
```

Implement Subsystem host consumer：

```text
@loomrealm/subsystem/host
    runSubsystem
    SubsystemLaunchContext
    SubsystemRuntimeControlPolicy
    Runtime Control role mapping using SubsystemRuntimeControlPeer
```

`DeadlineScheduler` 是 Platform capability；`helloDeadlineMs` / `frameDeadlineMs` 是 Subsystem Host role policy。M4 MUST NOT定义或保留 fake `SubsystemDataBinding` placeholder；其 exact Platform contract 与真实 Data application integration统一在 M8关闭。''')
          replace(path,
'''M4完成后允许表述：

```text
Subsystem Runtime/Frame Core Implemented
Subsystem Host Runtime Control consumer qualified
```''',
'''M4完成后允许表述：

```text
Platform Ports M4 contract real consumer qualified
Subsystem Runtime/Frame Core Implemented
Subsystem Host Runtime Control consumer qualified
```''')
          replace(path, '> 最近复核：2026-08-21', '> 最近复核：2026-08-27')

          # README.md: reflect dependency graph and current package status.
          path = 'README.md'
          replace(path,
'''@loomrealm/foundation ────────┐
                              ↓
@loomrealm/wire ───────→ @loomrealm/runtime-control
       │                      ↓
       │             Main / Subsystem Host''',
'''@loomrealm/foundation ─────→ @loomrealm/platform-ports ─────→ Main / Subsystem Host
       │
       ├──────────────────────┐
       │                      ↓
@loomrealm/wire ───────→ @loomrealm/runtime-control
       │                      ↓
       │             Main / Subsystem Host''')
          replace(path,
'''@loomrealm/subsystem/host
    → runtime-control + role-local integrations''',
'''@loomrealm/subsystem/host
    → platform-ports + runtime-control + role-local policy/integrations''')
          replace(path,
'''@loomrealm/runtime-control
    Implemented Baseline / Core Contract Frozen
    M3 local closure complete
    M4/M5 real role-consumer qualification pending

@loomrealm/data''',
'''@loomrealm/runtime-control
    Implemented Baseline / Core Contract Frozen
    M3 local closure complete
    M4/M5 real role-consumer qualification pending

@loomrealm/platform-ports
    Implemented Baseline / Core Boundary Frozen / M4 Slice Frozen
    M4 Subsystem real consumer qualification pending
    M5+ exact ports Evolving

@loomrealm/data''')

          # platform-ports DESIGN: package-local implementation is done; real consumer remains pending.
          path = 'packages/platform-ports/DESIGN.md'
          replace(path,
'''1. 已有真实 platform-neutral Core consumer；
2. Hostra/PWA 物理实现可不同但 abstract semantics 相同；''',
'''1. 已有冻结的 current-milestone platform-neutral Core consumer use-site，且 exact shape 已被该 milestone implementation gate 证明需要；真实 consumer qualification 必须在对应 milestone closure 前完成；
2. Hostra/PWA 物理实现可不同但 abstract semantics 相同；''')
          replace(path,
'''7. M4 subsystem/host consumes them''',
'''7. M4 subsystem/host real consumer qualification pending''')
          replace(path,
'''@loomrealm/platform-ports
    Core Boundary Frozen
    M4 Port Slice Frozen
    Implemented Baseline''',
'''@loomrealm/platform-ports
    Core Boundary Frozen
    M4 Port Slice Frozen
    Implemented Baseline
    M4 Subsystem real consumer qualification pending''')

          print('platform-ports documentation ownership closure applied')
          PY
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add README.md doc/30-implementation/package-architecture.md doc/30-implementation/repository-layout.md doc/30-implementation/phase-1-delivery-plan.md packages/platform-ports/DESIGN.md
          git diff --cached --check
          git commit -m "docs(platform-ports): close ownership and M4 consumer status"
          git push origin HEAD:feat/platform-ports-m4
