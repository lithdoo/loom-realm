name: Game Package conformance

on:
  pull_request:
    paths:
      - "packages/game-package/**"
      - "packages/wire/**"
      - "package.json"
      - "package-lock.json"
      - ".npmrc"
      - ".github/workflows/game-package.yml"
      - ".github/actions/setup-workspace/**"
  push:
    branches: [main]
    paths:
      - "packages/game-package/**"
      - "packages/wire/**"
      - "package.json"
      - "package-lock.json"
      - ".npmrc"
      - ".github/workflows/game-package.yml"
      - ".github/actions/setup-workspace/**"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  conformance:
    name: Node ${{ matrix.node }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        node: [20, 24]
    steps:
      - name: Check out repository
        uses: actions/checkout@v6
      - name: Setup workspace
        uses: ./.github/actions/setup-workspace
        with:
          node-version: ${{ matrix.node }}
      - name: Run Wire and Game Package conformance
        run: npm run test:game-package
      - name: Verify publishable package surface
        run: npm pack -w @loomrealm/game-package --dry-run
