# Subsystem Control Protocol v1 — Link Alias

> 状态：Historical Link Alias  
> 不定义协议语义  
> 当前权威：[Main ⇄ Subsystem Control Protocol v1](./subsystem-control-protocol-v1.md)

此路径仅用于保持已经冻结的 Frame / Call v1 文档中的历史相对链接可解析。

所有实现、设计与引用 MUST 使用：

```text
./subsystem-control-protocol-v1.md
```

本文没有独立 wire、schema、version、lifecycle 或 compatibility semantics；当前唯一 Subsystem Control v1 定义见上方权威契约。
