/**
 * Explicit Viewport State v1 conformance entry.
 * Package suites + test/viewport-core-vertical.test.mjs cover V-01..V-14.
 */
import test from "node:test";
import assert from "node:assert/strict";

test("viewport-state-v1 conformance runner is registered", () => {
  assert.ok(true);
});
